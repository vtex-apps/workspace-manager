type Query {
  getWorkspaces: GetWorkspaceResponse
}

type Mutation {
  "create a workspace"
  createWorkspace(name: String!, isProduction: String!): String
  "promote a workspace"
  promoteWorkspace(name: String!): String
}
